/**
 * A no-operation function that performs no action.
 * This function can be used as a placeholder or default value
 * where a function is required but no operation is necessary.
 *
 * @returns {void}
 */
export const noop = (): void => {};
